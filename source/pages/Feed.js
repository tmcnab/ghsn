import {Component} from "react";

/**
 * This page is auto-loading. As soon as the component scrolls towards the bottom it instructs the FeedStore to
 * increase the number of items it has loaded.
 */
export default class FeedPage extends Component {
    render () {
        return <div>FeedPage/div>
    }
}
